# 配置

```
查询默认路径
	npm config ls

// 创建下面两个文件夹，然后设置
npm config set prefix "E:\node\node_global"
npm config set cache "E:\node\node_cache"  


// 貌似不要用下面两个配置
环境变量 ：
	NODE_PATH: F:\node\node_modules
path 变量：
	F:\node\node_global
```

