

# npm

## 更改安装路径

```
启动终端
执行如下命令：
npm config set prefix "E:\repositories\node"
npm config set cache "E:\repositories\node"
配置好，使用npm install -g安装的模块将全部安装在该目录下。
配置环境变量
设置完全局安装目录后，然后在系统变量path中新增一个变量D:\Program Files\nodejs\node_global
 
```



